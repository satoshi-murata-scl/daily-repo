# Nixpacks: Node.js + better-sqlite3 のネイティブビルド用
# https://nixpacks.com/docs/configuration/file

providers = ["...", "node"]

[variables]
NIXPACKS_NODE_VERSION = "20"

# "..." で Node プロバイダの nodejs/npm を維持（nodejs は重複指定しない）
[phases.setup]
nixPkgs = ["...", "python311", "gcc"]
